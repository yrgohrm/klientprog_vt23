import { describe, it } from "vitest"

describe.only('nothing really...', () => {
    it('does nothing when we do nothing', () => {
        
    })

    it('does nothing again when we do nothing', () => {
        
    })

    it('does nothing once again when we do nothing', () => {
        
    })
})

describe('nothing again really...', () => {
    it('does nothing when we do nothing', () => {
        
    })
})
